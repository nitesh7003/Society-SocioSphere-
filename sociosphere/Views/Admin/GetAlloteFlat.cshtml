<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Alloted Flats - Sociosphere</title>
   
    <style>
        .alloted-flats-page {
            font-family: 'Roboto', sans-serif;
            background-color: #f0f4f8;
            color: #333;
            padding: 40px 0;
        }

        .alloted-flats-container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .alloted-flats-heading {
            font-family: 'Playfair Display', serif;
            color: #2c3e50;
            font-size: 2.5em;
            text-align: center;
            margin-bottom: 30px;
        }

        .alloted-flats-card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .alloted-flats-card-body {
            padding: 40px;
        }

        .alloted-flats-table {
            margin-bottom: 0;
        }

            .alloted-flats-table th {
                background-color: #4a90e2;
                color: white;
                font-weight: 500;
                text-transform: uppercase;
                font-size: 0.9em;
                letter-spacing: 1px;
                border: none;
            }

            .alloted-flats-table td {
                vertical-align: middle;
                border-color: #e0e0e0;
            }

        .alloted-flats-table-hover tbody tr:hover {
            background-color: #f5f5f5;
        }

        .alloted-flats-btn {
            border: none;
            padding: 8px 16px;
            border-radius: 5px;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .alloted-flats-btn-primary {
            background-color: #4a90e2;
            color: white;
        }

            .alloted-flats-btn-primary:hover {
                background-color: #3a7bd5;
                transform: translateY(-2px);
            }

        .alloted-flats-btn-danger {
            background-color: #e74c3c;
            color: white;
        }

            .alloted-flats-btn-danger:hover {
                background-color: #c0392b;
                transform: translateY(-2px);
            }

        .alloted-flats-alert {
            border-radius: 8px;
            font-weight: 500;
        }

        .alloted-flats-alert-dismissible .btn-close {
            padding: 1rem;
        }
    </style>
</head>
<body>
    <div >
        <div>
            <h2 class="alloted-flats-heading"><i class="fas fa-building me-3"></i>All Alloted Flats</h2>

            @if (TempData["SuccessMessage"] != null)
            {
                <div class="alert alloted-flats-alert alert-success alert-dismissible fade show" role="alert">
                    <i class="fas fa-check-circle me-2"></i> @TempData["SuccessMessage"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }
            @if (TempData["ErrorMessage"] != null)
            {
                <div class="alert alloted-flats-alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="fas fa-exclamation-circle me-2"></i> @TempData["ErrorMessage"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }

            <div class="card alloted-flats-card">
                <div class="card-body alloted-flats-card-body">
                    <div class="table-responsive">
                        <table class="table alloted-flats-table alloted-flats-table-hover">
                            <thead>
                                <tr>
                                    <th><i class="fas fa-hashtag me-2"></i>ID</th>
                                    <th><i class="fas fa-user me-2"></i>Name</th>
                                    <th><i class="fas fa-door-open me-2"></i>Flat No</th>
                                    <th><i class="fas fa-layer-group me-2"></i>Floor No</th>
                                    <th><i class="fas fa-building me-2"></i>Wing Name</th>
                                    <th><i class="fas fa-home me-2"></i>Flat Type</th>
                                    <th><i class="fas fa-calendar-alt me-2"></i>Move-in Date</th>
                                    <th><i class="fas fa-calendar-check me-2"></i>Allot Date</th>
                                    <th><i class="fas fa-cogs me-2"></i>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var flat in Model)
                                {
                                    <tr>
                                        <td>@flat.Id</td>
                                        <td>@flat.name</td>
                                        <td>@flat.flatno</td>
                                        <td>@flat.floorno</td>
                                        <td>@flat.wingname</td>
                                        <td>@flat.flattype</td>
                                        <td>@flat.moveindate.ToString("yyyy-MM-dd")</td>
                                        <td>@flat.allotdate.ToString("yyyy-MM-dd")</td>
                                        <td>
                                            <form asp-action="DeleteAlloteFlat" method="post" style="display:inline;">
                                                <input type="hidden" name="id" value="@flat.Id" />
                                                <a asp-action="EditAllot" asp-route-id="@flat.Id" class="btn alloted-flats-btn alloted-flats-btn-primary me-2">
                                                    <i class="fas fa-edit me-1"></i> Edit
                                                </a>
                                                <button type="submit" class="btn alloted-flats-btn alloted-flats-btn-danger" onclick="return confirm('Are you sure you want to delete this alloted flat?');">
                                                    <i class="fas fa-trash me-1"></i> Delete
                                                </button>
                                            </form>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>